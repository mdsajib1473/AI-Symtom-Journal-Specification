# Web framework - pinned to the 5.x series per Agent.md ("Django 5.x").
django>=5.0,<6.0

# PostgreSQL driver
psycopg2-binary

# Loads environment variables from .env
python-dotenv

# HTTP client for the Groq chat-completions API
requests

# Serves static files in production (Render)
whitenoise

# Production WSGI server (Render)
gunicorn

# IANA time zone data (Windows and slim Linux images have no system tz db)
tzdata

# Themed Django admin (research dashboard appearance only).
# PyPI distribution is django-jazzmin; the importable app is "jazzmin".
django-jazzmin
